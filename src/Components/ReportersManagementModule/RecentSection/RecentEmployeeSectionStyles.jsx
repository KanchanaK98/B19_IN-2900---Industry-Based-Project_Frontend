import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  gridContainer: {
    paddingLeft: 35,
    paddingRight:35,
    justifyItems: "center",
  },
  card: {
    borderRadius: 15,
    marginBottom: 15,
      
    padding: 25,
    marginLeft: 9,
    minWidth: 320,
  },
  empDetails: {},
  avatar: {
   
  },
}));

export default useStyles;
